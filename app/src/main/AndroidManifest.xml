<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="dlei.forkme">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/forkme_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/forkme_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- <activity android:name=".gui.activities.LogInActivity"> -->
        <!-- <intent-filter> -->
        <!-- <action android:name="android.intent.action.MAIN" /> -->


        <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
        <!-- </intent-filter> -->
        <!-- </activity> -->

        <activity
            android:name=".gui.activities.SplashScreenActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.hardikgoswami.oauthLibGithub.OauthActivity"/>
        <activity android:name=".gui.activities.TrendingRepositoriesActivity" />
        <activity android:name=".gui.activities.LogInActivity" />
    </application>
    <uses-permission android:name="android.permission.INTERNET"/>
</manifest>